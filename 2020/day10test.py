from day10 import iterate

if __name__ == "__main__":

    # assert iterate([1,2,3,4], 0) == 4, "That didn't work."
    assert iterate([1,2], 0) == 1, "That didn't work."
    assert iterate([1,2,3],0) == 1, "That didn't work."